# Raw metadata placeholder

Stage permitted public metadata here only after verifying access rules and logging source URLs and fetch dates. No full standard PDFs may be downloaded from portals. User-supplied licensed content requires an explicit, documented ingestion path before storage or indexing.
