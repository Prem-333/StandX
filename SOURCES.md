# Sources audit log

The canonical append-only log is [docs/SOURCES.md](docs/SOURCES.md). This root entry point and the canonical document satisfy both requested source-log locations without maintaining divergent copies.

User-provided requirements and local environment inspection are not live internet lookups. Phase 0 uses those inputs and synthetic data only.

Phase 1 live research, policy checks, access failures, and documentation-validator provenance are recorded in the canonical log above.

Phases 2–4 add the 20 individual metadata captures, access/feed checks, publisher model cards and asset hashes, current Indic leaderboard response, dependency versions, and Qdrant documentation. Fetch dates in the canonical log use UTC; local benchmark measurements are recorded separately and are not internet facts.
