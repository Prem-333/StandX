# Configuration placeholders only; no services are configured in Phase 0.
# Copy to .env when configuring the stack. Never commit credentials.
APP_ENV=development
DATA_MODE=synthetic
DATABASE_URL=
QDRANT_URL=
NEO4J_URI=
NEO4J_USERNAME=
NEO4J_PASSWORD=
EMBEDDING_PROVIDER=
# Choose a model only after a live model-card/license check and audit entry.
EMBEDDING_MODEL=
